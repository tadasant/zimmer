# Kamal production secrets. Values come from the deploy environment (GitHub Actions
# secrets on your private production repo) -- nothing secret is committed here, only the mapping.
KAMAL_REGISTRY_USERNAME=$KAMAL_REGISTRY_USERNAME
KAMAL_REGISTRY_PASSWORD=$KAMAL_REGISTRY_PASSWORD

SECRET_KEY_BASE=$PROD_SECRET_KEY_BASE
# Decrypts config/credentials/production.yml.enc (mcp_secrets). Unlike staging,
# production supplies a real key so credential-bearing MCP servers can authenticate.
RAILS_MASTER_KEY=$PROD_RAILS_MASTER_KEY
API_KEYS=$PROD_API_KEYS

# Managed Postgres password (doadmin). Distinct from SECRET_KEY_BASE -- the managed
# cluster is long-lived and its password is not derived from any app secret.
DATABASE_PASSWORD=$PROD_DB_PASSWORD

# The prod self-session MCP server's key for calling this instance's own REST API
# (first entry of the comma-separated API_KEYS; the deploy workflow derives it).
#
# Both names carry the SAME derived value, on purpose. The AGENT_ORCHESTRATOR_* ->
# ZIMMER_* rename is a cross-repo lockstep; the reader (SelfSessionInjector#self_target)
# reads the ZIMMER_ name now, and prod's encrypted mcp_secrets carries it too. Keep the
# AGENT_ORCHESTRATOR_* name here as the dual-set safety net until it is retired in a
# later step -- dropping it now, mid-rename, would resolve the key to empty and 401
# every session's MCP server.
AGENT_ORCHESTRATOR_PROD_API_KEY=$PROD_SELF_API_KEY
ZIMMER_PROD_API_KEY=$PROD_SELF_API_KEY

# The operator SSH private key agent sessions authenticate with (the ssh-* MCP servers
# are ssh2 publickey clients). BASE64 of the OpenSSH private key, not the PEM itself:
# Kamal hands env vars to Docker through an env-file, and a Docker env-file cannot
# carry a newline -- a raw PEM would arrive truncated at its first line break.
# OperatorSshKeyProvisioner decodes it into ~/.ssh/zimmer_operator_ed25519 (0600) at boot.
#
# Same keypair as staging -- but note what it does NOT open. This key is deliberately
# NOT authorized on production itself: the sessions holding it RUN on production, and it
# is a root key, so authorizing it would let an agent take down the box executing it. It
# is the identity a prod session uses to reach STAGING, obs, and the CI runner. Root on
# production is for humans and for the off-box orchestrator only.
# See docs/operate/ssh-access.md#who-is-authorized-where.
#
# PROD_OPERATOR_SSH_KEY must be set as a GitHub Actions secret on the private production
# repo; its value is the base64 in the 1Password item "Zimmer operator SSH key
# (zimmer-production-operator)".
ZIMMER_OPERATOR_SSH_KEY=$PROD_OPERATOR_SSH_KEY

# The Google Parameter Store resolver credential -- the key to the FIRST link of the
# ${VAR} resolution chain (SecretProviders). Only the credential is secret; the store's
# ADDRESS (ZIMMER_PARAMS_PROJECT_ID / _LOCATION) is in env.clear in
# config/deploy.production.yml on purpose, so a misconfiguration is diagnosable.
#
# BASE64 of the service-account key JSON, not the JSON itself. Same reason as
# ZIMMER_OPERATOR_SSH_KEY above, and it is worth being precise about the mechanism
# because the failure is quiet: Kamal escapes a real newline to the two characters \n
# (Ruby's String#dump) and Docker's --env-file parser unescapes NOTHING, so those two
# characters arrive literally. `gcloud iam service-accounts keys create` writes
# PRETTY-PRINTED JSON, so it lands in the container with \n between its fields and
# JSON.parse rejects it -- and the app degrades cleanly rather than crashing, which
# means the store just silently never turns on. ParameterStore::ServiceAccount.parse
# accepts base64 or raw JSON; base64 is the one that cannot be got wrong.
#
# PROD_ZIMMER_PARAMS_RESOLVER_SERVICE_ACCOUNT_KEY_JSON must be set as a GitHub Actions
# secret on the private production repo (tadasant-internal). Its value is:
#   base64 -w0 < /tmp/zimmer-secrets-resolver.json
# See docs/operate/secrets-parameter-store.md#5-deliver-the-key-to-zimmer.
#
# Optional, like RAILS_MASTER_KEY: unset simply means the store link is absent and every
# ${VAR} resolves from encrypted credentials exactly as before. Nothing raises.
ZIMMER_PARAMS_RESOLVER_SERVICE_ACCOUNT_KEY_JSON=$PROD_ZIMMER_PARAMS_RESOLVER_SERVICE_ACCOUNT_KEY_JSON

# The WRITE half, held by a SEPARATE service account with the write roles the
# resolver above deliberately does not have. It backs exactly one surface -- the
# Inference page's Pi tab, which creates/rotates/deletes OPENROUTER_API_KEY -- and
# it is plumbed here so that opening that path is a deploy rather than a shell on
# the box.
#
# Optional in the same way, and the failure mode is deliberate: unset means the
# Pi tab reports the write path closed and names the permissions it is missing.
# It never silently writes somewhere else.
#
# PROD_ZIMMER_PARAMS_WRITER_SERVICE_ACCOUNT_KEY_JSON is a GitHub Actions secret on
# the private production repo, like the resolver key. Its value is:
#   base64 -w0 < /tmp/zimmer-secrets-writer.json
# The service account and its two roles are created by a human -- no agent in this
# deployment can. See
# docs/operate/secrets-parameter-store.md#a-writer-identity-for-the-pi-tab.
#
# CliSpawnEnv clears this from every spawned session, for a stronger reason than
# it clears the resolver: a session holding it could rewrite every Zimmer secret.
ZIMMER_PARAMS_WRITER_SERVICE_ACCOUNT_KEY_JSON=$PROD_ZIMMER_PARAMS_WRITER_SERVICE_ACCOUNT_KEY_JSON

# Observability (optional): empty values leave the app's initializers as no-ops.
SENTRY_DSN_BACKEND=$SENTRY_DSN_BACKEND
OTEL_LOGS_EXPORTER_ENDPOINT=$OTEL_LOGS_EXPORTER_ENDPOINT
OTEL_LOGS_EXPORTER_BEARER_TOKEN=$OTEL_LOGS_EXPORTER_BEARER_TOKEN
