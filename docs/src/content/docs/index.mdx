---
title: Zimmer
description: Self-hostable orchestration for AI coding agents — sessions as isolated clones, a lifecycle you can reason about, and a catalog that wires each agent's context.
template: splash
head:
  - tag: title
    content: Zimmer — Self-hostable orchestration for AI coding agents
hero:
  tagline: Self-hostable orchestration for AI coding agents. You give it a task and a repo; it hands you back a pull request to approve, or a specific question about why it couldn't.
  image:
    file: ../../assets/logo-dark.svg
  actions:
    - text: Get started
      link: /start/local/
      icon: rocket
    - text: What Zimmer is
      link: /intro/what-zimmer-is/
      variant: minimal
      icon: right-arrow
    - text: GitHub
      link: https://github.com/tadasant/zimmer
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard, Aside } from '@astrojs/starlight/components';

Zimmer is open-source, standards-built, and made for a single circle of trust — you,
or a couple, or business partners. You stay in control of what runs and what merges;
Zimmer handles the toil of getting an agent from a prompt to a verified PR.

## Why run it

<CardGrid>
  <Card title="It isn't a bet on one vendor" icon="seti:plan">
    The agent harness is a [registry behind a contract](/extend/agent-harness/) — Claude
    Code and Codex both ship today, and you pick the model per session (Claude or OpenAI).
    [MCP servers](/air/mcp-servers/) are a JSON entry, not a code change. Nothing in the design
    assumes whose agent wins, which is the point: nobody knows yet, and the orchestration
    layer outlives the answer.
  </Card>
  <Card title="Bounded on purpose" icon="puzzle">
    There is largely one way to do each thing in Zimmer: a session is an [isolated
    clone](/intro/philosophy/#1-a-session-is-its-own-isolated-clone), its context comes from
    [the catalog](/air/overview/), and its
    lifecycle is [one state machine](/sessions/lifecycle/). The top-level architecture is
    meant to stay stable, so by default you don't need to know how any of it is implemented —
    and when something does break, you drill down into a part you can name.

    The failure mode this avoids is the usual one for a hand-rolled agent rig: a self-learning
    agent let loose on a machine, accreting layers of glue that nobody understands, breaking
    regularly, with no obvious road back. Zimmer would rather be a system you can hold in your
    head.
  </Card>
  <Card title="A boring stack on one cheap box" icon="seti:db">
    Rails 8, PostgreSQL, Redis, GoodJob, Hotwire. It's a Docker image that Kamal ships to
    any Linux host you can SSH into, and a single small droplet runs the whole thing. No
    per-seat bill, no exotic infra to keep alive, nothing you haven't operated before.
  </Card>
  <Card title="You sign in — you don't paste keys" icon="approve-check">
    The goal is to juggle as few long-lived secrets as possible. Claude and Codex
    authenticate against your own account over OAuth, `gh` uses the device flow, and MCP
    servers [register themselves and refresh their own tokens](/auth/mcp-oauth/) — dynamic
    client registration, PKCE, automatic refresh.

    Which matters on the bad day. Agents read and write a lot of text, and sooner or later a
    credential lands somewhere it shouldn't — a log line, a transcript, a pasted diff. If it
    was a long-lived API key, you get to choose between a permanent exposure and spending the
    next five hours manually rotating every copy of it. If it was a short-lived token the
    runtime already rotates, it expires on its own and you re-auth in a browser.
  </Card>
  <Card title="It follows you off the laptop" icon="rocket">
    Install Zimmer as a PWA and it web-pushes your phone when a session finishes, fails, or
    stops to ask you something. Agent work that takes an hour shouldn't need you to sit and
    watch it for an hour.
  </Card>
  <Card title="Built for work that outlives your attention" icon="setting">
    Pinned sessions and categories, "blocked by" dependencies between sessions, live PR and
    CI status on each session, search across full transcripts, opt-in
    [heartbeats](/sessions/lifecycle/) that nudge an idle agent, and
    [goals](/sessions/goals/) that spell out what "done" actually requires.
  </Card>
  <Card title="The honest exception" icon="warning">
    Cloning works against any git remote, but the PR, CI, and review-comment automation is
    GitHub-specific today — it shells out to `gh`. If you live on GitLab, that half of
    Zimmer isn't there for you yet.
  </Card>
</CardGrid>

## Start here

Three ways in, shortest first. Pick the one that matches what you want right now.

<CardGrid>
  <LinkCard
    title="Run it locally →"
    href="/start/local/"
    description="The fastest look: bundle install, db:setup, bin/dev, and you're at localhost:3000. Ruby, Postgres, and Redis on your machine."
  />
  <LinkCard
    title="Drive your first session →"
    href="/start/first-session/"
    description="Point a session at a repo, give it a task, and watch it go from prompt to pull request. What the states mean and where a session can stall."
  />
  <LinkCard
    title="Self-host your own →"
    href="/operate/self-hosting/"
    description="Stand up a real instance: one droplet, tailnet-only, Terraform provisions the host and Kamal deploys the app. Do it yourself, or hand a prompt to an agent."
  />
</CardGrid>

New to the idea? Read [What Zimmer is](/intro/what-zimmer-is/) first, then come back.

## What it does

<CardGrid>
  <Card title="It runs a real agent, headless" icon="server">
    A session is a real Claude Code or Codex process running against a real clone of a
    real repo. It commits, it pushes, it opens a PR, and it hands the result back for you
    to approve.
  </Card>
  <Card title="It has a lifecycle" icon="random">
    `waiting → running → needs_input → archived`. Every session sits in one of those
    states, and the state machine decides the next move.
  </Card>
  <Card title="It wires the context" icon="puzzle">
    Skills, MCP servers, agent roots, hooks, plugins, and references are resolved from a
    catalog by [AIR](/air/overview/) and injected into the clone before the agent starts.
  </Card>
</CardGrid>

<Aside type="tip" title="These docs are blunt on purpose">
The premise is that you can read this site instead of the code. That only works if it tells
you what the code would, so it names Zimmer's real bugs and limitations — inline where they
bite, and aggregated on [Known limitations](/limitations/). Where the code and the old docs
disagreed, the code wins and the contradiction is written down. Where the code was genuinely
ambiguous, you get an "unclear — needs confirmation" note instead of a guess.
</Aside>

## Go deeper

Once you've seen it run, these explain why it's shaped the way it is.

<CardGrid>
  <LinkCard
    title="Philosophy"
    href="/intro/philosophy/"
    description="The opinions baked into Zimmer: isolated clones, closed-loop autonomy, MCP as a blast radius, PR-as-review-gate, and why the catalog exists."
  />
  <LinkCard
    title="Architecture"
    href="/intro/architecture/"
    description="Rails app, GoodJob workers, agent subprocesses, Postgres, Redis, and the diagram of how a prompt becomes a running process."
  />
  <LinkCard
    title="AIR"
    href="/air/overview/"
    description="The separate open-source project that resolves an agent's context from a catalog. This is the canonical write-up."
  />
  <LinkCard
    title="REST API"
    href="/extend/rest-api/"
    description="Every endpoint, re-derived from the routes, including the six resources the old reference forgot."
  />
  <LinkCard
    title="Known limitations"
    href="/limitations/"
    description="The bugs, the brittleness, and the things nobody has decided yet. Read this before you trust anything else."
  />
</CardGrid>
