# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# Empty on purpose: tests create the enqueued messages they need inline. The file
# still exists so the table is truncated when fixtures load.
