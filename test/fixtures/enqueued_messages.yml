# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# Empty fixture file - enqueued messages are created dynamically in tests
# This file exists to ensure the enqueued_messages table is properly truncated
# when fixtures are loaded, preventing foreign key violations from stale data.
