# PR media — Connectors page UX

Screenshots and a screen recording for the Connectors page PR. They live on this
orphan branch, not in the PR's diff, because the screenshot bucket's service
account key was rejected (`invalid_grant: Invalid JWT Signature`) for the whole
of that session and the PR description has to embed them from somewhere.

Nothing here is code. Delete the branch once the PR is merged.
