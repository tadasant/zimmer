{
  "$schema": "https://pulsemcp.github.io/air/schemas/air.schema.json",
  "name": "zimmer-catalog",
  "description": "In-image AIR catalog used in production/staging. Points at the artifact indexes shipped in the image (resolved offline via the bundled @pulsemcp/air-cli). Mirrors air.json; kept separate so the image can pin its own catalog sources without touching the dev/test config.",
  "gitProtocol": "https",
  "extensions": [
    "@pulsemcp/air-adapter-claude",
    "@pulsemcp/air-secrets-env"
  ],
  "skills": [
    "./skills/skills.json"
  ],
  "mcp": [
    "./mcp.json"
  ],
  "roots": [
    "./roots.json"
  ],
  "references": [
    "./references/references.json"
  ],
  "hooks": [
    "./hooks/hooks.json"
  ],
  "plugins": [
    "./plugins/plugins.json"
  ]
}
